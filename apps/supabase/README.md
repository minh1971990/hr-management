Supabase workspace for the HR Management app.

- Migrations: `apps/supabase/migrations/`
- Edge Functions: `apps/supabase/supabase/functions/`

Most testers only need to run the frontend (`apps/web`). Supabase setup/deployments are for maintainers.